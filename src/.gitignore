

### Dependencies ###############################################################
# The node_modules directory contains all of your project's dependencies.
# It can be very large and should always be generated locally by running `npm install` or `yarn install`.
node_modules/

### Build output ###############################################################
# These directories contain the compiled, production-ready code.
# They are generated by running a build command (e.g., `npm run build`).
build/
dist/
.next/

### Environment variables ######################################################
# NEVER commit environment variables. They often contain sensitive secrets and API keys.
# A template file like .env.example should be committed instead.
.env
.env.local
.env.development.local
.env.production.local
.env.test.local

### Logs #######################################################################
# Log files generated by npm or yarn.
npm-debug.log*
yarn-debug.log*
yarn-error.log*

### Testing ####################################################################
# Test coverage reports generated by tools like Jest.
coverage/

### Editor & System files ######################################################
# These are files specific to your local editor or operating system and should not be shared.

# VSCode
.vscode/

# JetBrains (WebStorm, etc.)
.idea/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db

### Miscellaneous ##############################################################
# Other temporary or generated files.
.pnp.*
.npm